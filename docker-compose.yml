version: '2'
services:
 mongodb:
  image: "mongo"
  ports:
  - "27017:27017"
 backend:
  build: ./backend/
  ports:
   - "4000:4000"
  depends_on:
   - mongodb
 frontend:
   build: ./frontend/
   ports:
    - "3000:3000"
   depends_on:
    - backend